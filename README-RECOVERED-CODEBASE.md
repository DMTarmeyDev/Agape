# Agape recovered full codebase

This archive was recovered from the actual saved Agape project artifacts in the user's ChatGPT Library on 15 September 2026.

## Recovered source

- `core/` - Agape / DMT Core V3.1 Early Alpha R8, 127 original source/config/test files before recovery additions.
- `components/agape-document-studio/` - Document Studio source extracted from the R31.9 one-file installer and upgraded with the saved R1.6 full-rebuild patch to R31.10.
- `components/agape-systems-engine/` - Systems/Work Engine embedded source archive, upgraded with the R1.6 patch to R1.3.
- `components/agape-artifacts/` - Artifacts R3.1 embedded source payload.
- `components/agape-communications-hub/` - Communications Hub R5 Python source plus its original installer.
- `components/agape-main-host/` - Main Host R1 Windows service/worker installer source.
- `windows-native-r6/` - Windows native/WebView2 host source and source references.
- `windows-native-r7/` - Windows 11 main host source.
- `provenance/` - the earlier `AGAPE-COMPLETE-CORE-0c9b1285` source for comparison.
- `patches-r1.6/` - exact patch scripts extracted from the saved R1.6 full-rebuild source reference.

## Important

This is a recovered composite source tree, not a claim that one historical ZIP contained every component in this exact layout. The components were developed and installed separately in the original project. Original installers are retained beside recovered source where available.

See `RECOVERY-TEST-RESULTS.txt`, `RECOVERY-NOTES.txt`, and `RECOVERY-MANIFEST.json`.
