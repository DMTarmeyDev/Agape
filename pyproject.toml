[project]
name = "agape-mainframe"
version = "5.5.0"
description = "Human-first Agape core shell and capability manager"
requires-python = ">=3.11"

[project.scripts]
agape-mainframe = "agape_mainframe.server:main"

[project.optional-dependencies]
writing-quality = ["pyspellchecker>=0.8"]
secure-keys = ["keyring>=25,<26"]
