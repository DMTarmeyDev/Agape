# Agape V5.6.1 R2.6.1 - Project-first acceptance repair

- Let Agape decide ignores explicitly negated coding/mobile/tool terms.
- Strong document-output intent wins before generic development keywords.
- Starting a freshly planned project clears stale Saved Project selection and returns Source to Paste text.
- Document Studio dynamically bounds Windows output folder/filename components and, when the parent output root itself is too long, redirects generated artifacts to a short persistent per-root Windows output directory.
- Document creation uses an Agape-owned runtime temp folder and emits an explicit output-path diagnostic if a path is still excessive.
